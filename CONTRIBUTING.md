# Contributing

Keep the rendering engine destination-neutral. Add destination-specific facts only under `examples/` or a user's independent content pack. Run `npm run check`, `npm run build`, and `npm run test:sites` before opening a pull request. Do not include secrets, server configuration, remote image hotlinks, or unverified routes represented as roads.
